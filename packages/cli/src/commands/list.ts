// `onda list` — print the catalog grouped by category.
//
// M1 placeholder. Real implementation lands in M5.

export async function runList(_args: string[]): Promise<void> {
  console.error(
    'onda list: not implemented yet — coming in techspec 006 M5.\n' +
      'See https://github.com/degueba/onda/blob/main/docs/techspecs/006-cli/roadmap.md',
  );
  process.exit(2);
}
